<?xml version="1.0" encoding="utf-8"?>

<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:orientation="vertical"
    tools:context=".MainActivity"
    android:id="@+id/drawer_layout">

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#FFD104"
        android:textAlignment="center"
        app:headerLayout="@layout/menu_header"
        app:itemIconSize="50dp"
        app:itemIconTint="@android:color/black"
        app:itemTextAppearance="@style/TextAppearance.AppCompat.Large"
        app:menu="@menu/main_menu"
        tools:ignore="RtlCompat" ></com.google.android.material.navigation.NavigationView>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <fragment
            android:id="@+id/arfragment"
            android:name="com.google.ar.sceneform.ux.ArFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent" ></fragment>

        <ImageButton
            android:id="@+id/imageButton2"
            android:layout_width="168dp"
            android:layout_height="197dp"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginStart="5dp"
            android:layout_marginTop="4dp"
            android:layout_marginEnd="627dp"
            android:layout_marginBottom="1079dp"
            android:backgroundTint="#818181"
            android:scaleType="centerInside"
            app:srcCompat="@drawable/menubutton_png" ></ImageButton>


    </RelativeLayout>


</androidx.drawerlayout.widget.DrawerLayout>